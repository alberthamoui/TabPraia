name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Atualizar versão no package.json
        run: |
          $version = "$env:GITHUB_REF_NAME".TrimStart("v")
          npm version $version --no-git-tag-version

      - name: Instalar dependências
        run: npm install

      - name: Rebuild módulos nativos
        run: npm run rebuild

      - name: Gerar electron/env.js com variáveis de ambiente
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_APP_SECRET: ${{ secrets.VITE_APP_SECRET }}
        run: |
          "module.exports = { API_URL: '$env:VITE_API_URL', APP_SECRET: '$env:VITE_APP_SECRET' }" | Out-File -FilePath electron/env.js -Encoding utf8

      - name: Build e publicar
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_APP_SECRET: ${{ secrets.VITE_APP_SECRET }}
        run: npm run release
